**Кейс 99-41116 – Итоговый отчет**

1. **Документ (audit_report_99-41116.docx)**
   - В документе отсутствуют реплики и таблицы.

2. **SQL‑запросы**
   - `query_01.sql` содержит опасный оператор DELETE без WHERE, что может привести к потере всех данных в таблице `analytics.client_risk`.
   - `query_02.sql`:
     * SELECT * с условием LIKE на поле comment может раскрыть чувствительные данные (например, пароли).
     * Синтаксическая ошибка – отсутствует закрывающая скобка после `count(1`.

3. **Python‑скрипты**
   - `analysis_01.py`: перезаписывает входной CSV без резервного копирования → риск потери исходных данных.
   - `analysis_02.py`: обращается к неопределённой переменной `top_client`, что вызовет исключение; отсутствует проверка существования файла и корректности столбцов.

**Рекомендации**
- Переписать DELETE‑оператор с условием WHERE или использовать транзакцию/тестовую копию.
- Удалить SELECT * и уточнить нужные поля, а также убрать LIKE на чувствительные данные.
- Исправить синтаксическую ошибку в `query_02.sql`.
- В скриптах добавить резервное копирование файлов перед записью и обработку исключений.

**Ключевая фраза:** REPORT_DONE